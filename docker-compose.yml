services:
  web:
    build: .
    image: gofast-wish-web:latest
    container_name: gofast-wish-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
